version: '2'

volumes:
  nextcloud:
  db:
  proxy:

services:
    
    proxy:
    image: jwilder/nginx-proxy:alpine
    container_name: nextcloud-proxy
    ports:
      - 80:80
      - 443:443
    volumes:
      - proxy:/etc/nginx/conf.d:rw
      - proxy:/etc/nginx/vhost.d:rw
      - proxy:/usr/share/nginx/html:rw
      - proxy:/etc/nginx/certs:ro
      - proxy:/etc/localtime:ro
      - proxy:/tmp/docker.sock:ro
    restart: always
    
  db:
    image: mariadb
    restart: always
    volumes:
      - db:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=N3xtCl0ud!
      - MYSQL_PASSWORD=nextcloud
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud

  app:
    image: nextcloud
    ports:
      - 8080:80
    links:
      - db
    volumes:
      - nextcloud:/var/www/html
    restart: always
